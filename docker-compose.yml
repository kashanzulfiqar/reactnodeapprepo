version: '3.8'

services:
  backend:
    build:
       context: ./backend
       dockerfile: Dockerfile
    image: nodebackendimg:latest
    ports:
      - "80:5000"
    environment:
      - PORT=80

  #frontend:
   # build: 
    #  context: ./frontend
     # dockerfile: Dockerfile
    #image: reactfrontendimg:latest
    #ports:
     # - "5173:5173"
    #depends_on:
     # - backend
