apiVersion: 1

contactPoints:
  - orgId: 1
    name: Slack Alerts
    receivers:
      - uid: slack-notifier
        type: webhook # 👈 Change from 'slack' to 'webhook'
        settings:
          url: "${SLACK_WEBHOOK_URL}"
